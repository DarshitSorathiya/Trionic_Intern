/**
 * Placeholder for Week 3:
 * Parses an incoming stream of raw markdown and identifies `[CITE:XXX]` tokens.
 */
export function parseStreamForCitations(rawMarkdownChunk: string) {
  // Parsing logic goes here
  return rawMarkdownChunk;
}
