const fs = require('fs'); const path = require('path'); function walk(dir) { let results = []; const list = fs.readdirSync(dir); list.forEach(function(file) { file = dir + '/' + file; const stat = fs.statSync(file); if (stat && stat.isDirectory()) { results = results.concat(walk(file)); } else { if (file.endsWith('.ts') || file.endsWith('.tsx')) { results.push(file); } } }); return results; } const files = walk('.'); files.forEach(file => { if (file.includes('node_modules') || file.includes('.next')) return; let content = fs.readFileSync(file, 'utf8'); if (content.includes(' // eslint-disable-line @typescript-eslint/no-explicit-any')) { content = content.split(' // eslint-disable-line @typescript-eslint/no-explicit-any').join(''); fs.writeFileSync(file, content); console.log('Reverted: ' + file); } });
